// STUB: audio — a specialist builder fills this in. Contract: export create(ctx)
// returning { update(dt, ctx) } (plus any API other systems need).
export function create(ctx) {
  return { update(dt, ctx) {} };
}
